import React from 'react'; import './App.css'; import { Link, useNavigate } from 'react-router-dom'; const HistoryPage = ({ resumes, setSelectedResumeId, setResults }) => { const navigate = useNavigate(); const sortedHistory = [...resumes].sort((a, b) => a.analysis.match_score - b.analysis.match_score); return (<div className='page-content'><div className='card' style={{width: '100%'}}><h2>Full Analysis History ({sortedHistory.length})</h2>{sortedHistory.length === 0 ? <p>No history found.</p> : sortedHistory.map(r => (<div key={r.id} className='resume-item' onClick={() => { setSelectedResumeId(r.id); setResults(r.analysis); navigate('/'); }}><span style={{cursor: 'pointer'}}>{r.fileName} - Match: {r.analysis.match_score}%</span><span style={{fontSize: '0.8rem', color: 'var(--gray)'}}>{new Date(r.id).toLocaleString()}</span></div>))}</div></div>);}; export default HistoryPage;
